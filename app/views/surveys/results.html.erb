<h1>Results are In</h1>

<% if Survey.today.any? %>
  <h3>These peeps are coming: <%=Survey.result_participants%></h3><br>
  <h3>This is where we going: <%=Survey.result_winner.first%> (<%=Survey.concensus? ? "All" : Survey.result_winner.last %> Agreed)</h3>
<% else %>
  <h3>No Surveys Submitted Today</h3>
<%end%>
<br>
<%= link_to 'Back to List', restaurants_path %>
