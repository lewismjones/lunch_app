<div class="translucent">

<h3>This is how we choose the lunch. Consider this your invite, dummy.</h3>
<br>
<h2> <%= link_to '>> Get started <<', new_survey_path %> </h2>
<br>

<article class="frontrunners">
  <% if @frontrunners.any? %>
    <span>Frontrunner(s):</span>
    <div>⍚ <% @frontrunners.each do |restaurant| %>
       <%=link_to restaurant.name, restaurant.website, {target: "blank"}%> ⍚
      <%end%>
    </div>
  <%else%>
    <h3>No one has responded yet 😩 </h3>
  <%end%>
</article>

</div>
